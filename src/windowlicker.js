/**
 * Default settings:
 * small <  600
 * large >= 600
 *
 * Set:
 * windowlicker.set({
 *   small: [0, 599]
 * });
 */

var matchMedia = require('./matchMedia');
var s = document.head;


console.log(s);
